---
platform: linux

image_resource:
  type: registry-image
  source:
    repository: ruby
    tag: 3.2

inputs:
- name: java-buildpack
- name: buildpack-version

outputs:
- name: built-buildpack

run:
  path: java-buildpack/ci/tasks/build-buildpack/run.sh
